types:
  DnsSubenumType:
    enum:
      - BRUTE
      - PASSIVE
  DnsSubenumReport:
    properties:
      domain: string
      enumerationType: DnsSubenumType
      subdomains: list<string>
      errors: optional<list<string>>
