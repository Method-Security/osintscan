types:
  DnsSubenumType:
    enum:
      - BRUTE
      - PASSIVE
  DnsSubenumReport:
    properties:
      domain: string
      enumerationType: DnsSubenumType
      subdomains: optional<list<string>>
      errors: optional<list<string>>
